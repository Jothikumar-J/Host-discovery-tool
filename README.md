# Host-discovery-tool
This is an Active Scanner—it sends out various types of probes to see who "answers," allowing  an administrator or security researcher to map out every connected device on a network.  It acts as a lightweight network scanner that identifies active devices on a local area network  (LAN).
